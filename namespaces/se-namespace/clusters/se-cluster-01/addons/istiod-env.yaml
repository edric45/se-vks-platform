#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.subset({"kind":"Deployment","metadata":{"name":"istiod"}})
---
spec:
  template:
    spec:
      containers:
      #@overlay/match by=overlay.subset({"name":"discovery"})
      - name: discovery
        #@overlay/match missing_ok=True
        env:
        #@overlay/append
        - name: OVERLAY_TEST
          value: "hello-from-ytt"

