# Cluster: se-cluster-01
#
# Values below are modelled on working clusters on this Supervisor:
#   classRef  builtin-generic-v3.7.0   (newest offered here)
#   version   v1.35.6+vmware.2         (in use by ks115-user/cluster-01)
#
# vmClass and storageClass MUST already be bound to se-namespace in vCenter.
# Verify before applying:
#   kubectl -n se-namespace get virtualmachineclass
#   kubectl -n se-namespace get storageclass
apiVersion: cluster.x-k8s.io/v1beta2
kind: Cluster
metadata:
  name: se-cluster-01
  namespace: se-namespace
spec:
  # Required. The Supervisor rejects a Cluster without clusterNetwork.services:
  #   "spec.ClusterNetwork.Services must be defined"
  # These are the values every working cluster on this Supervisor uses.
  clusterNetwork:
    services:
      cidrBlocks:
      - 10.96.0.0/12
    pods:
      cidrBlocks:
      - 192.168.156.0/20
    serviceDomain: cluster.local
  topology:
    classRef:
      name: builtin-generic-v3.7.0
      namespace: vmware-system-vks-public
    version: v1.35.6+vmware.2
    controlPlane:
      replicas: 1
    workers:
      machineDeployments:
      - class: node-pool
        name: se-cluster-01-np01
        replicas: 2
    variables:
    # best-effort-large (4 vCPU / 16Gi). Raised from best-effort-medium
    # (2 vCPU / 8Gi), which could not hold a rolling istiod restart: the new
    # replicas sat Pending with "Insufficient cpu" because requests, not usage,
    # drive scheduling. Applies to control plane and workers alike -- vmClass is
    # a cluster-wide topology variable.
    - name: vmClass
      value: best-effort-large
    - name: storageClass
      value: vsan-default-storage-policy
