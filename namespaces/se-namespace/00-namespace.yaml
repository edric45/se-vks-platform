# vSphere Namespace: se-namespace
#
# This creates the namespace object only. Before a Cluster can be scheduled
# into it, vCenter must attach a storage policy and at least one VM class --
# neither has a manifest form. See README.md in this directory.
#
# Labels/annotations such as vSphereClusterID, nsx.vmware.com/vpc_network_config
# and aviinfrasetting.ako.vmware.com/name are generated by vCenter. Do not
# author them here.
apiVersion: v1
kind: Namespace
metadata:
  name: se-namespace
