# guest-cluster-auth-service 1.4.8+vmware.1-tkg.1

| Property | Type | Default | Description |
|---|---|---|---|
| `authServicePublicKeys` | string | `` | Auth Service public keys to verify VC issued JWTs |
| `ceritificate` | string | `` | Certificate for TLS |
| `privateKey` | string | `` | Private key for TLS |
